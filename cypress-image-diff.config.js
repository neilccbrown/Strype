const config = {
    SCREENSHOTS_DIR: "tests/cypress/expected-screenshots",
    // Turn this to false if you are developing a new test, but make
    // sure you do not commit that.  This should always be true in the
    // repository version.
    FAIL_ON_MISSING_BASELINE: true,
};

module.exports = config;
